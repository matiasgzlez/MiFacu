/**
 * Re-export useColorScheme from ThemeContext
 * This allows components that use useColorScheme to automatically use the theme context
 */
export { useColorScheme } from '../context/ThemeContext';
